"""
URL patterns for the registry app.
Views will be added in Phase 3 when models and logic are implemented.
"""
from django.urls import path

app_name = "registry"

urlpatterns = [
    # Phase 3: views will be registered here
    # e.g. path("", views.dashboard, name="dashboard"),
]
